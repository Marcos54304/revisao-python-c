#include <stdio.h>

int main() {

int a, b, soma;

printf("Primeiro valor: ");
scanf ("%d", &a);

printf("Segundo valor: ");
scanf ("%d", &b );

soma = a + b;
printf ("soma: ");

printf ("Soma: %d", soma);

    return 0;
}